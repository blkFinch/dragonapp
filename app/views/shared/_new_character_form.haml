= form_with(model: [ @campaign, Pcharacter.new ], local: true) do |form|
  = form.label :name 
  %br
  = form.text_field :name
  %br
  = form.label :icon
  = form.file_field :image, class: 'form-control'
  = form.submit "Create!", class: "btn btn-primary"
